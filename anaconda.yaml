anaconda_config_version: 1
upstream_sources:
  - pypi:
      identifier: pint
      strip_prefix: v
    packages: [pint]
  - github:
      identifier: hgrecco/pint
      strip_prefix: v
    packages: [pint]
